#include "main.h"


/**
* description: _abs-computes the absolu
* @n: the int to check
* Return: the absolute value of int
*/
	int _abs(int n)
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}
